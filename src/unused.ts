/**
 * Update the testing collision cursor, to show whether it's currently colliding with anything.
 */
// function updateCursor(x: number, y: number) {
//   const isSolid = checkCollision(x, y);
//   cursor.setAttributeNS(null, 'fill', isSolid ? '#f00' : '#0f0');
//   cursor.setAttributeNS(null, 'cx', '' + x);
//   cursor.setAttributeNS(null, 'cy', '' + y);
// }